export declare class TransliterateHelper {
    static transliterate(input: string, config?: {
        spaceReplacement?: string;
    }): string;
    static reverseTransliterate(input: string): string;
}
