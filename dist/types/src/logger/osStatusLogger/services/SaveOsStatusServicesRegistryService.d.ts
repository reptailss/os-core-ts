export declare class SaveOsStatusServicesRegistryService {
    static saveServicesRegistry({ serviceKey, endpoints }: {
        serviceKey: string;
        endpoints: string[];
    }): Promise<void>;
}
