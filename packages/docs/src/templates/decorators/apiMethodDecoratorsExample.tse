import {{{Controller}},{{Get}},{{Post}},{{Put}},{{Delete}},{{AppError}}} from 'os-core-ts'

type User = {
	name: string
	age: string
}

@{{Controller}}()
class UserController {

	private user: User | null = null

	@{{Get}}('/get-user')
	public getUser(): User {
		if (!this.user) {
			throw new {{AppError}}('Not Found', {
				errorKey: 'NOT_FOUND_ERROR',
			})
		}
		return this.user
	}

	@{{Post}}('/save-user')
	public saveUser(user: User): User {
		this.user = user

		return this.user
	}

	@{{Put}}('/update-user-name')
	public updateUserName(userName: string): User {
		if (!this.user) {
			throw new {{AppError}}('Not Found', {
				errorKey: 'NOT_FOUND_ERROR',
			})
		}
		this.user = {
			age: this.user.age,
			name: userName,
		}

		return this.user
	}

	@{{Delete}}('/delete-user')
	public deleteUser(): boolean {
		this.user = null

		return true
	}
}
