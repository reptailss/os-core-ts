import {{{Controller}},{{Get}},{{UserInfo}},{{PtpCoreAuthDec}}} from 'os-core-ts'

@{{Controller}}()
export class UserController {

	@{{Get}}('/get-user-name')
	public getUserName(
		@{{PtpCoreAuthDec}}(['admin']) userInfo: {{UserInfo}},
	): string {
		return userInfo.user_name
	}
}