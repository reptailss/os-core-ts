import {{{Controller}},{{Get}},{{UserInfo}},{{PtpClientAuthDec}}} from 'os-core-ts'

@{{Controller}}()
export class UserController {

	@{{Get}}('/get-user-name')
	public getUserName(
		@{{PtpClientAuthDec}}(['admin']) userInfo: {{UserInfo}},
	): string {
		return userInfo.user_name
	}
}