import {{{Controller}},{{Get}},{{UserInfo}},{{AuthDec}}} from 'os-core-ts'

@{{Controller}}()
export class UserController {

	@{{Get}}('/get-user-name')
	public getUserName(
		@{{AuthDec}} userInfo: {{UserInfo}},
	): string {
		return userInfo.user_name
	}
}